node_modules/
.env
playwright-report/
**/playwright/.auth/
**/playwright/.state/
test-results/

# Deploy poller: local box config (panel credentials) and its log
scripts/deploy.config.json
scripts/deploy.log

# World backups. Never committed -- they are large, and their absence on a
# fresh clone is exactly what makes the panel's "no backup yet" indicator
# meaningful.
minecraft-panel/backups/
